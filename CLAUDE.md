# 이 저장소에서 Claude가 지킬 규칙

## 코드 설명 방식 (사용자 요청으로 규칙화, 2026-09-08)

사용자는 파이썬 입문자. 코드 설명은 **개념·비유가 아니라 예시 데이터로 실행 흐름을 한 줄씩 트레이스**한다.

1. 작은 예시 입력을 먼저 명시 ("폴더에 a.jpg, b.pdf가 있다고 치고")
2. 가능하면 실제로 실행해서 진짜 값을 찍고 그 값으로 설명
3. 코드 블록 각 줄에 `# → 값` 주석으로 그 순간의 상태를 적기
4. 반복문은 "1번째 반복: p = …" 회차별로
5. `return`은 호출자 표현식에서 반환값으로 치환되는 과정을 안쪽부터 보여주기
6. 핵심 분기에 ★ 표시 + "여기가 X였다면 → 결과 Y" 대조 한 줄
7. 끝에 "정리하면:" 불릿 2~3개

## 과제 진행 방식

- week1/README.md 규칙 우선: 과제 코드는 힌트 먼저. 사용자가 시도한 뒤 막히면 코드를 준다.
- 과제 하나 끝날 때마다 커밋 유도.
