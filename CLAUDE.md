# 이 저장소에서 Claude가 지킬 규칙

## 코드 설명 방식 (사용자 요청으로 규칙화, 2026-09-08)

사용자는 파이썬 입문자. 코드 설명은 **개념·비유가 아니라 예시 데이터로 실행 흐름을 한 줄씩 트레이스**한다.

1. 작은 예시 입력을 먼저 명시 ("폴더에 a.jpg, b.pdf가 있다고 치고")
2. 가능하면 실제로 실행해서 진짜 값을 찍고 그 값으로 설명
3. 코드 블록 각 줄에 `# → 값` 주석으로 그 순간의 상태를 적기
4. 반복문은 "1번째 반복: p = …" 회차별로
5. `return`은 호출자 표현식에서 반환값으로 치환되는 과정을 안쪽부터 보여주기
6. 핵심 분기에 ★ 표시 + "여기가 X였다면 → 결과 Y" 대조 한 줄
7. 끝에 "정리하면:" 불릿 2~3개

## 과제 진행 방식

- week1/README.md 규칙 우선: 과제 코드는 힌트 먼저. 사용자가 시도한 뒤 막히면 코드를 준다.
- 과제 하나 끝날 때마다 커밋 유도.

## 과제(Day N) 안내 형식 (사용자 요청으로 규칙화, 2026-09-08)

"다음 단계 진행해줘"류 요청에는 아래 형식으로 상세하게 안내한다. 코드 정답은 주지 않고 힌트까지만.

1. **한 줄 목적 요약**: 이 Day가 하는 일을 한 문장으로
2. **단계 2~4개**, 각각 예상 소요시간 표기. 단계마다:
   - 구체적 행동 (어느 파일 어느 부분, 브라우저에서 뭘 클릭)
   - 왜 중요한지 / 여기서 배우는 것 한두 문장
   - 코드는 "1줄: ~해서 resp에 담기" 식 자연어 힌트
   - 실행 흐름은 위 트레이스 규칙으로 예시값과 함께
3. **확인 단계**: 붙여넣을 터미널 명령 + 기대 출력 + 그 출력의 의미 한 줄
4. **커밋 단계**: 명령과 메시지까지
5. "오늘 안 해도 되는 것"(다음 Day 몫)을 명시해 범위를 잘라 준다
6. "막히면 어디서 막혔는지 말해달라"로 마무리

## 회고·설명 과제 (2026-09-09 추가)

- 회고 질문에는 반드시 "어떻게 답하면 되는지" 예시 한 줄을 붙인다. 아직 안 배운 내용을 전제로 한 질문은 내지 않는다.
- 회고·설명은 사용자가 직접 쓴다. Claude는 채점과 실마리만. 사용자가 "감이 안 온다"고 하면 답이 아니라 질문의 뜻과 답 형식 예시를 다시 설명한다.
- 수준 판단은 관찰한 행동을 근거로 하고, "감이 안 와서 물은 것"과 "위임"을 구분한다.
